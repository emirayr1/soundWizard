from setuptools import setup, find_packages

setup(
    name="sound_wizard",
    version="0.1.0",
    description="A Python library for audio processing",
    author="AYR",
    packages=find_packages(),
    python_requires=">=3.7",
    install_requires=[
        
    ],
)
